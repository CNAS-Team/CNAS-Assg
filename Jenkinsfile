pipeline {
    agent any

    stages {
        stage('Clone Test') {
            steps {
                echo 'Jenkins successfully loaded the Jenkinsfile from GitHub.'
                sh 'ls -la'
            }
        }
    }
}
